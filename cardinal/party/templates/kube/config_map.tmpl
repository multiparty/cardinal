apiVersion: v1
kind: ConfigMap
metadata:
  name: {{CONFIGMAP_NAME}}
  labels:
    app: {{POD_NAME}}

data:
  workflow: {{WORKFLOW_NAME}}
  config_encoded: {{CONG_CONFIG}}
  curia_config_encoded: {{CURIA_CONFIG}}
